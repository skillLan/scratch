platform :ios, '8.0'
inhibit_all_warnings!
use_frameworks!

# 通过pod 使用三方库时,必须指定明确的版本号!方便同期开发的用户使用相同的三方,方便后续更新!
# 编辑该文件时,必须使用专业的编辑器.不允许使用记事本编辑,避免导致的乱码错误引发的问题.

target 'ScratchDemo' do

 pod 'SnapKit', '~> 4.2.0'                 # UI布局
 
end
